---
import type { Locale } from '@/i18n/config';
const { locale } = Astro.props as { locale: Locale };
const message = { es: 'Vídeo pendiente de incorporar', en: 'Video pending', fi: 'Video lisätään myöhemmin', da: 'Video afventer' }[locale];
---
<p class="video-pending">{message}</p>
