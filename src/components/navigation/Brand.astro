---
import logo from '@/assets/brand/bionatura-logo.png';
import { pathFor, type Locale } from '@/i18n/config';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const descriptor: Record<Locale, string> = {
  es: 'productos de temporada',
  en: 'seasonal products',
  fi: 'kausituotteet',
  da: 'sæsonvarer',
};
---

<a class="brand" href={pathFor(locale, 'home')}>
  <img src={logo.src} width="344" height="324" alt="" />
  <span class="brand__wordmark">
    <strong>Bionatura</strong>
    <small>Fuengirola · {descriptor[locale]}</small>
  </span>
</a>
